firebird
========

Installation
============
Install pip

    apt-get install python-pip

Install flask
    
    pip install flask

Using database load script:
    sqlite3 <dbname> < loaddatabase.sql
